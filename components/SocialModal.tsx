'use client';

// SocialModal era parte del flujo "minimize" del diseño OS.
// Con el nuevo diseño Portfolio, este modal ya no se usa.
// Los links sociales están integrados en AboutJD (Bento Grid).
export default function SocialModal() {
  return null;
}
